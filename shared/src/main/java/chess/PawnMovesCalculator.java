package chess;

public class PawnMovesCalculator extends ChessMove_calculator{

    public PawnMovesCalculator(ChessPosition position, ChessBoard board) {
        super(position, board); // Call the base class constructor
    }
}
